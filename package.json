{
  "name": "cube2",
  "version": "0.1.0",
  "type": "module",
  "description": "A little incremental game for game jam",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "alpinejs": "^3.13.5"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "vite": "^5.1.0"
  }
}
